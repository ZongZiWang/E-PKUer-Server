<% url = (action_name == "new" ? {:action=>"create", :controller=>"dish_comments"} : {:action=>"update", :controller=>"dish_comments"})%>
<%= form_for([@dish, @dish_comment], url: url) do |f| %>
  <% if @dish_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dish_comment.errors.count, "error") %> prohibited this dish_comment from being saved:</h2>

      <ul>
      <% @dish_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dish_id %>
    <%= f.number_field :dish_id %>
  </div>
<br />
  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>
<br />
  <div class="field">
    <%= f.label :evaluation %>
    <%= f.text_field :evaluation %>
  </div>
<br />
  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>
<br />
  <div class="actions">
    <%= f.submit "Upload the Comment Info", :class=>"submit"%>
  </div>
<br />
<% end %>
