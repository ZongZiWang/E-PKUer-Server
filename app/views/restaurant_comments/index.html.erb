<body class="thrColElsHdr">
<div id="container">
  <div id="header">
	<h1>汴地吃货 - 食堂点评列表</h1></div>
	<div id="mainContent">
<table>
  <tr>
	<th>id</th>
    <th>Restaurant</th>
    <th>User</th>
    <th>Evaluation</th>
    <th>Cost</th>
    <th>Time</th>
    <th>Recommendation Dishes</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <%
  odd_or_even = 0
  %>
<% @restaurant_comments.each do |restaurant_comment| %>
 <%
  odd_or_even = 1-odd_or_even
  %>
  <tr class = "ListLine<%= odd_or_even %>" >
    <td><%= restaurant_comment.id %></td>
	<td><% if restaurant_comment.restaurant %><%= link_to restaurant_comment.restaurant.name, restaurant_comment.restaurant %><% end %></td>
	<td><% if restaurant_comment.user %><%= link_to restaurant_comment.user.name, restaurant_comment.user %><% end %></td>
    <td><%= restaurant_comment.evaluation %></td>
    <td><%= restaurant_comment.cost %></td>
    <td><%= restaurant_comment.created_at %></td>
    <td><%= restaurant_comment.recommendation_dishes %></td>
    <td><%= restaurant_comment.content %></td>
    <td><%= link_to 'Show', restaurant_comment_path(@restaurant, restaurant_comment) %></td>
    <td><%= link_to 'Edit', edit_restaurant_comment_path(@restaurant, restaurant_comment) %></td>
    <td><%= link_to 'Destroy', restaurant_comment_path(@restaurant, restaurant_comment), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
<p>
<%= link_to 'New Restaurant comment', new_restaurant_comment_path(@restaurant) %>
</p>
</div>
<div id ="footer">
<p>2012 北京大学信息科学技术学院© 王仲禹 阳泓吉 刘芸</p>
</div>
</div>
</body>

